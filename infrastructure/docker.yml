# Docker Configuration
# Generated for DemOne

tool:
  name: Docker
  version: 24.0+
  category: Infrastructure

project:
  name: DemOne
  description: DevOps project generated with NoCodeDevOps
  author: Fred
  version: 1.0.0
  repository: https://github.com/sprintcorp/ops-test

configuration:
  # Add your Docker configuration here
  # Refer to the tool's documentation for available options
  
examples:
  - name: Node.js Application
    description: Multi-stage Node.js application build
    config: {
    "baseImage": "node:18-alpine",
    "packages": [
        "git",
        "python3"
    ],
    "ports": [
        3000
    ]
}

best_practices:
  - Use multi-stage builds for smaller images
  - Implement proper layer caching
  - Use .dockerignore to exclude files
  - Run containers as non-root user
